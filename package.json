{
  "name": "react-to-beast-skill",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "ISC",
  "engines": {
    "node": ">=22.22.2"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "npm test"
  }
}
